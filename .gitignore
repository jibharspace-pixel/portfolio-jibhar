node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment — ne jamais committer
.env
.env.local
.env.*.local

# Données et fichiers uploadés — ne pas committer
data.json
uploads/
